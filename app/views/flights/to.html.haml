%h1 To
= form_tag('date', method: 'get') do
	= hidden_field_tag :from_id, params[:from_id]
	= select_tag :to_id, options_from_collection_for_select(@airports, :id, :code), class: "form-control col-md-offset-3", style: "width: 500px"
	= submit_tag 'Next', class: "btn btn-primary btn-lg text-center", style: "margin-top: 20px"